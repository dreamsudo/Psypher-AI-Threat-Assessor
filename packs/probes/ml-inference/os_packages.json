{
  "id": "os_packages",
  "tier": "passive",
  "applies_to": ["host", "inference_endpoint"],
  "observes": ["os_package"],
  "run": {
    "type": "shell",
    "via": "ssh",
    "cmd": "if command -v dpkg-query >/dev/null 2>&1; then dpkg-query -W -f='${Package}==${Version}\\n' 2>/dev/null | sed 's/==[0-9]*:/==/'; elif command -v rpm >/dev/null 2>&1; then rpm -qa --qf '%{NAME}==%{VERSION}-%{RELEASE}\\n' 2>/dev/null; fi",
    "raw_cap": 4194304
  },
  "parse": { "regex": "^(?P<os_package>[A-Za-z0-9][A-Za-z0-9.+_-]*?)==" }
}
